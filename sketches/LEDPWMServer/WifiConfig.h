// WiFi access point settings
const char *ssid = "ExampleSSID";
const char *password = "3Xample!pa55wd";

// Device's static IP
IPAddress static_ip(192, 168, 1, 10);

// Router IP (used for gateway and DNS)
IPAddress router_ip(192, 168, 1, 1);

// Netmask (default 255.255.255.0)
IPAddress subnet(255, 255, 255, 0);

